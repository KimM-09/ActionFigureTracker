# Example environment variables for the Notes App
# Copy this file to `.env` and fill in real values before running.

# Backend
MONGO_URI=mongodb://<username>:<password>@<host>:<port>/<database>
PORT=5001
NODE_ENV=development